# Simple PowerShell helper to deploy the monorepo to Railway using the Railway CLI
# Usage: Open PowerShell in repo root and run: `.












# To run detached, use `railway up --build --detach` if preferred.railway up --build# Build and bring up services via Railway (this will build Docker images)railway init --name code-sync-railpack# Initialize a project (if not already). Replace name if desired.railway login# Login (interactive)ailpack-app\deploy.ps1`